#include <string>
#include <fstream>

#include <stdlib.h>

#include "util.h"

#ifndef TWO
#define TWO

/* functions are disabled
namespace two{

  bool add(std::string file_path, std::string project_name);

  bool add_dir(std::string dir_path, std::string project_name);

  bool sub(std::string file_name, std::string project_name);

  bool sub_dir(std::string dir_name, std::string project_name);

  bool add_new(std::string file_name, std::string project_name);
}
*/
#endif
